function section(title,items,open=false){if(!items.length)return'';return`<details class="result-section" ${open?'open':''}><summary>${title}</summary><div>${items.map(item=>`<p class="result-line">${item}</p>`).join('')}</div></details>`}
export function renderResult(result){if(!result.valid)return`<h2 class="result-title">Illegal combat</h2>${section('Block legality',result.legality,true)}<p>Correct the assignments before resolving combat.</p>`;const sections=result.steps.flatMap((step,index)=>[section(step.name,step.events,index===0),section('State-based actions',step.stateEvents,index===0)]).join('');return`${sections}${section('Final result',result.final.concat(`Damage to opposing player: ${result.playerDamage}`),true)}`}
